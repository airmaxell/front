import { Route } from "@angular/router";
import { RequestStatisticComponent } from "./request-statistic.component";


export const RequestStatisticRoutes: Route[] = [
    {
        path: "request-statistic",
        component: RequestStatisticComponent
    }
]