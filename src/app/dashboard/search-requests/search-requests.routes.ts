import { Route } from '@angular/router';
import { SearchRequestsComponent } from './search-requests.component';

export const SearchRequestsRoutes: Route[] = [
    {
      path: 'search-requests',
      component: SearchRequestsComponent
    }
];
