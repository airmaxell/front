import { Route } from '@angular/router';
import { SentRequestsComponent } from './sent-requests.component';

export const SentRequestsRoutes: Route[] = [
    {
      path: 'sent-requests',
      component: SentRequestsComponent
    }
];
